apiVersion: v1
kind: Service
metadata:
  name: f5-demo
  labels:
    app: f5-demo
spec:
  ports:
  - name: f5-demo
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: f5-demo